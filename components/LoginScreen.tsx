import { Box, Text } from "@gluestack-ui/themed";

const LoginScreen = () => {
  return (
    <Box flex={1} bg="red" width='100%'>
      <Text>Login Screen</Text>
    </Box>
  );
};

export default LoginScreen;
