import { Box, Text } from "@gluestack-ui/themed";

const HomeScreen = () => {
  return (
    <Box flex={1} overflowY="scroll" bg="red" width='$full'>
      <Text>Home Screen</Text>
    </Box>
  );
};

export default HomeScreen;
